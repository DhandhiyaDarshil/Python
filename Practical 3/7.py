# Create a tuple with numbers
number_tuple = (10, 20, 30, 40, 50)

# Print one item from the tuple (e.g., the third item)
index_to_print = 2  # Indexing starts from 0, so 2 corresponds to the third item
print("Item at index", index_to_print, ":", number_tuple[index_to_print])
